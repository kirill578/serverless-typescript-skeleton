This is an example service that is using:

* Serverless
    * webpack (bundle the project)
    * serverless-offline (test locally)
* Inversify - for dependency injection
* Testing via:
    * mocha - framework
    * sinon - mocks, asserts
    * chai - asserts
* TypeScript
* regular JS code